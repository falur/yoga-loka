<?php

declare(strict_types=1);

namespace App\Shared\Infrastructure\Persistence\Cycle;

/**
 * Маркер: класс осознанно выполняет set-based запись (UPDATE/INSERT/DELETE одним запросом)
 * для массовых операций без доменных инвариантов. По умолчанию это запрещено
 * (rules.md «Репозиторий — только чтение»; всё через Entity + EntityManager).
 * PHPStan-правило DisallowSetBasedWriteRule разрешает прямую запись на DatabaseInterface
 * только в классах с этим маркером.
 *
 * Применять, только когда выполнены ОБА блока условий (rules.md «Set-based запись — только
 * через маркер SetBasedWrite»):
 *  - необходимость: набор затрагиваемых строк не ограничен доменом сверху / горячий путь /
 *    чистый штамп колонки по набору;
 *  - безопасность: у строки нет доменного инварианта, переход тривиален и идемпотентен,
 *    затронутые строки не используются дальше как живые Entity в этом же запросе.
 */
interface SetBasedWrite {}
