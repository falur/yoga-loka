<?php

declare(strict_types=1);

namespace App\Shared\Infrastructure\Spiral\Http\Access;

/**
 * Маршрут не объявил требование доступа ни одним зарегистрированным атрибутом.
 *
 * Это ошибка разработчика, а не пользователя: забытая декларация не должна молча открывать
 * доступ, поэтому запрос до контроллера не доходит. Имя метода маршрута попадает в журнал общего
 * обработчика ошибок API, а наружу уходит общий ответ 500 без подробностей.
 */
final class RouteAccessNotDeclaredException extends \LogicException {}
