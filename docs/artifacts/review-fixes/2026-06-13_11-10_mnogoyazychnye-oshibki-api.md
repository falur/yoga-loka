---
review: docs/reviews/2026-06-13_10-50_mnogoyazychnye-oshibki-api.md
date: 2026-06-13 11:10
status: done
---

# Фиксы по ревью: Многоязычные ошибки API — перевод на границе + выбор языка по Accept-Language

Scope строго ограничен прямым указанием пользователя: применены РОВНО три замечания — 1, 6, 7.
Замечание 5 (рабочий файл `todo`) и замечания 2, 3, 4, а также пункт сверки с планом (пометка
об отклонении) — намеренно отложены по решению пользователя, не из-за оценки их полезности.

## Применённые правки
| # | Замечание | Файлы | Тесты | Статус |
|---|-----------|-------|-------|--------|
| 1 | Мусорный хвост в журнале выполнения + неактуальные числа `423`/`23` | `docs/executions/2026-06-11_22-56_mnogoyazychnye-oshibki-api.md` | — (правка документации) | ✓ применено |
| 6 | Английское `supported` в сообщении исключения `LocaleConfig` | `app/src/Shared/Infrastructure/Configuration/Locale/LocaleConfig.php` | — (текст сообщения не ассертится) | ✓ применено |
| 7 | `foreach`+`continue`+ранний `return` вместо коллекционного пайплайна в `LocaleMiddleware` | `app/src/Shared/Infrastructure/Framework/Middleware/LocaleMiddleware.php` | `tests/Unit/Shared/Infrastructure/Framework/Middleware/LocaleMiddlewareTest.php` (9 кейсов ✓, не менялся) | ✓ применено |
| 2 | Нет каталога `auth.php` для домена `auth` | — | — | — отложено по решению пользователя (scope 1/6/7) |
| 3 | `FakeTranslator` без `readonly` ради спая `lastDomain` | — | — | — отложено по решению пользователя (scope 1/6/7) |
| 4 | `translationDomain()` без валидации формата ключа | — | — | — не править (по решению ревью) + вне scope |
| 5 | Рабочий файл `todo` уходит в коммит | — | — | — отложено по решению пользователя (явно исключён) |
| план | Пометка об отклонении `translationDomain()` в файле плана | — | — | — отложено по решению пользователя (scope 1/6/7) |

## Детали правок

### Замечание 1 — журнал выполнения
- Удалён мусорный хвост в самом конце файла: пустой повторный заголовок `## Изменения в docs`
  и обе строки `</content>` / `</invoke>`.
- Содержательный раздел `## Изменения в docs` выше по файлу сохранён без изменений.
- Секция «## Финальная проверка»: устаревшие числа `423` теста и `23` теста пакета обновлены до
  актуальных `424` / `24`; добавлена явная пометка, что сводка отражает состояние после всех
  шагов (включая 5–6). Числа сверены с актуальным прогоном `make qa` (424 теста) и таблицей шагов 5–6.

### Замечание 6 — LocaleConfig
- `\sprintf('одна из supported (%s)', ...)` → `\sprintf('одна из поддерживаемых (%s)', ...)`.
  Сообщение исключения теперь полностью на русском по `rules.md:6`.

### Замечание 7 — LocaleMiddleware::matchSupportedLocale()
- `foreach` + `continue` + ранний `return` заменены на коллекционный пайплайн над
  `Illuminate\Support\Collection` (по `rules.md:19` чистые трансформации — collection-пайплайны):
  `Collection::make(...getAll())->filter(quality > 0)->map(primarySubtag)->first(∈ supported)`.
- `getAll()` уже отсортирован по quality по убыванию, поэтому `first()` возвращает самый
  приоритетный поддерживаемый язык или `null` — поведение и контракт `string|null` сохранены.
- Добавлены импорты `Illuminate\Support\Collection` и `Spiral\Http\Header\AcceptHeaderItem`.
- `LocaleMiddlewareTest` (9 кейсов, включая `en;q=0`, `*`, порядок по quality) остался зелёным
  без правок — рефакторинг не меняет контракт.

## Решения по optional
- **Принято:** 6, 7 — обязательны к применению по прямому указанию пользователя (scope 1/6/7);
  обе правки дёшевы, соответствуют `rules.md` (`:6`, `:19`) и не меняют поведение.
- **Отложено (по решению пользователя, не из-за оценки полезности):** 2, 3 (optional),
  пункт сверки с планом — пользователь явно ограничил scope тремя замечаниями. Это не отказ
  по существу: при следующем проходе их можно применить отдельно.
- **Не применено по явному исключению пользователя:** 5 (`todo`) — несмотря на статус
  «править обязательно» в ревью, пользователь явно исключил его из этого фикса.
- **Не править (по решению ревью):** 4 — `translationDomain()` без валидации формата ключа,
  осознанный компромисс.

## Финальная проверка
- **Тесты + coverage:** `make qa` (cs + PHPStan + один coverage-run PCOV) — ✓ зелёный.
  424 теста, покрытие 100.00% при пороге 100%.
- **Линтер/стиль:** входит в `make qa` (codestyle) — ✓ No errors.
- **PHPStan:** входит в `make qa` — ✓ No errors.
- **Заметки:** 1 PHPUnit Notice — предсуществующий нотис в
  `ProcessMediaHandlerTest::testStoresConversionDimensionsFromProcessorResult` (мок без
  expectations), задокументирован в журнале, не связан с этими правками, гейт `make qa` exit 0.
