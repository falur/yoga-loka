---
title: Модуль Posts/Tags/User — прикладной и презентационный слой (5-й круг, фокус на фиксах 4-го круга)
date: 2026-06-19 21:30
target: git diff HEAD + untracked
plan: docs/plans/2026-06-18_17-42_posts-application-presentation.md
mode: strict
score: 97
status: final
meta_reviewers: [plan-check (sonnet), architecture-check (sonnet), rules-check (sonnet), quality-check (sonnet), codex]
---

# Ревью: Модуль Posts/Tags/User — прикладной и презентационный слой (5-й круг)

## Оценка

**97/100.** Реализация плана полная, фиксы четвёртого круга корректны и не дали регрессий: новый
общий сервис разрешения упоминаний разведён по трём режимам осмысленно, дешёвая проверка
существования и guard автора покрыты тестами, мягкая рассылка при публикации не ломает прежнее
поведение и не дублирует уведомления. Новых реальных проблем не найдено; остаток до 100 — это
естественный потолок для большого изменения, прошедшего четыре круга, а не конкретный дефект, под
который стоило бы снижать оценку.

## Проблемы сверки с планом

Явных проблем по плану не найдено. План реализован по всем пяти фазам; каждый HTTP-роут покрыт
интеграционным тестом, решения 1–27 соблюдены.

## Замечания

Явных проблем в коде не найдено. Фиксы четвёртого круга проверены по коду всеми мета-ревьюерами и
кросс-CLI и подтверждены как корректные, без регрессий (три режима `MentionRecipientResolver`,
`CheckUsersExist`/`countByIds`, guard `requireAuthor`, мягкая рассылка `post_mention` при публикации).
Технические уточнения по обоснованиям «здесь проблемы нет» зафиксированы в разделе изменений после
мета-ревью; самостоятельных дефектов под них нет.

## Рекомендации

- **Править обязательно:** нет.
- **На усмотрение автора:** нет.

## Изменения после мета-ревью

### После plan-check / architecture-check / rules-check / quality-check

- **+ Добавлено:** ничего. Реальных новых проблем не подтверждено.
- **~ Изменено:** ничего.
- **− Убрано:** ничего.
- **Отклонено:**
  - «Добавить пункт: `MentionRecipientResolver::resolveRequired` не защищён от дублей изнутри —
    при будущем вызывающем без `array_unique` метод даст ложный 422; добавить
    `array_unique` в начало метода» (quality-check `+`, тип robustness, «на усмотрение») —
    отклонено. Проверено по коду: оба текущих вызывающих (`PostContentComposer:117` и
    `CommentComposer:43`) дедуплицируют список через `\array_values(\array_unique(...))` до
    вызова; контракт зафиксирован `@param list<string>`. Текущего дефекта нет, это
    спекулятивная защита под несуществующего будущего вызывающего — искусственный пункт для
    финального круга, ревью ведётся только по проблемам.
  - «Переформулировать тезис про дедуплицированные списки (`resolveRequired`), т.к. он создаёт
    впечатление полного отсутствия проблемы» (quality-check `~`) — отклонено. Формулировка была
    фактически верна и проверена по коду: оба вызывающих действительно передают дедуплицированные
    списки; тезис не утверждал внутреннюю защиту метода. (Сам подтверждающий блок про
    `MentionRecipientResolver` позже убран при чистке ревью до формата «только по проблемам», см.
    раздел после кросс-CLI.)
  - «`RepostPostHandler::handle()` ~47 строк, формально превышает ориентир ~40»
    (quality-check) — отклонено. Метод плотно структурирован, без вложенности, максимально
    делегирован в `PostContentComposer`; правило задаёт ориентир «~40», искусственное дробление
    ухудшит читаемость. Не дефект.
  - Англицизмы «стейджинг/стейджит/батч» в комментариях (rules-check) — не восстановлено:
    осознанно отклонено в 1-м круге как устоявшийся технический жаргон по всему проекту
    (Notifications/Outbox/Shared), материально новых аргументов нет.
- **plan-check запускался:** план указан в front matter (`docs/plans/2026-06-18_17-42_...`);
  все 5 фаз, 15 роутов схемы B, 7 видов уведомлений, межмодульные сценарии, конфиг User и
  read-фильтры репозиториев подтверждены реализованными; каждый HTTP-роут покрыт интеграционным
  тестом. Фиксы 4-го круга (три режима `MentionRecipientResolver`, `CheckUsersExist`/`countByIds`,
  guard `requireAuthor`, мягкая рассылка `post_mention` при публикации) проверены по коду всеми
  четырьмя ролями как корректные, без регрессий.

### После соседнего CLI (codex)

- **+ Добавлено:** ничего.
- **~ Изменено (чистка ревью до формата «только по проблемам», по `~` codex):** из секций
  «Проблемы сверки с планом» и «Замечания» убраны длинные подтверждающие блоки «проверено и
  подтверждено как корректное» — это был отчёт о выполненной работе, а не проблемы. Секции сжаты до
  короткой констатации «проблем не найдено» с указанием, что обоснования «здесь проблемы нет»
  зафиксированы в этом разделе. Вместе с блоками ушли две неточные формулировки, на которые указал
  codex: (1) рассуждение про soft-deleted пользователей теперь не нужно как тело замечания — по сути
  это была лишь техническая согласованность путей `countByIds`/`findByIds`, а не бизнес-тезис о
  допустимости упоминать удалённых; (2) фраза «актор существует по определению» — корректность
  держится на том, что актор публикации = аутентифицированный владелец уже существующей записи
  (FK `posts.user_id` `RESTRICT` не даёт записи без существующего автора), а не на абстрактном
  «по определению»; прямой вызов handler-а с несуществующим `authUserId` вне HTTP-потока дал бы 404 —
  это не дефект сценария.
- **− Убрано:** подтверждающие блоки (см. выше) как неформат ревью.
- **Отклонено:**
  - «`+` Добавить тестовый пробел: `PublishPostHttpTest` не покрывает именно мягкий путь
    `resolveExisting`, когда сохранённый получатель исчез к моменту публикации» (codex) — отклонено.
    На текущей модели данных этот путь недостижим (FK `post_mentions.user_id` `delete: CASCADE`
    убирает строку упоминания при физическом удалении, а soft-delete не фильтруется `findByIds`),
    отдельной непокрытой ветки внутри `resolveExisting` нет (метод просто возвращает существующие
    профили, покрытие 100%). Сценарный тест на «исчезнувшего получателя» осознанно отложен в 4-м
    круге до появления политики «скрывать удалённых из профилей» — материально новых аргументов нет.
  - «`~` Метаданные strict/final» (codex) — учтено самим завершением проверки: `status` переведён в
    `final`, `codex` добавлен в `meta_reviewers`.
- **`?` codex:** `make test`/`make phpstan` соседним CLI не запускались. Зелёный прогон `make qa`
  (1125 тестов, 100% покрытие, PHPStan level max — без ошибок) зафиксирован в отчёте фиксов 4-го круга
  `docs/review-fixes/2026-06-19_20-45_posts-application-presentation.md`; диффом с тех пор затронуты
  только эти артефакты ревью.
