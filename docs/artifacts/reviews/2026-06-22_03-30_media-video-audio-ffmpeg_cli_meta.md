+ Добавить замечание: видео-тест не проверяет фактические кодеки H.264 + AAC. Сейчас проверяются MIME, размеры и пропорции, но не `codec_name` потоков. Код, похоже, корректен через `X264` по умолчанию, но контракт плана тестом не закреплён.

+ Добавить замечание: прямые вызовы `ffmpeg` для волны и постера используют `ffmpegTimeoutSeconds`, но игнорируют `ffmpegThreads`. Если настройка нужна для ограничения нагрузки, часть ffmpeg-вызовов её обходит.

+ Добавить недостающий тест: для audio нет проверки, что два аудио-профиля дают 422. Код это отвергает через `count($plan->audio) !== 1`, но план явно требовал сценарии для video/audio.

− Убрать замечание про «асимметрию дубликатов image vs video/audio» как проблему кода. Для video/audio требование «ровно один профиль» уже закрывает и дубликаты, и несколько профилей; отдельная проверка уникальности была бы мёртвой.

− Убрать или сильно понизить замечание про хрупкость пропорций видео-теста. Для текущей фикстуры 320x240 в рамке 640x480 равенство 4:3 не зависит от спорного округления. Это риск будущей смены фикстуры, а не текущий дефект.

~ Переформулировать замечание про пустой PCM: это не «падающая ветка», а молчаливое превращение аномального пустого результата ffmpeg в валидную плоскую волну. Лучше писать как «нужен явный выбор: ошибка процессора или документированная тишина».

? `make test` и `make phpstan` я не запускал; вывод основан на чтении diff, untracked-файлов Media, плана, правил и релевантного кода.
