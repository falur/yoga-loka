# Кросс-CLI мета-ревью (codex, gpt-5.3-codex, exit 0)

Итоговое сообщение Codex (исходный потоковый лог обрезан до финального ответа):

---

+ Добавить проблему по правилам языка: в новых комментариях и документации много англицизмов в русском тексте. Примеры: `guard`, `no-op`, `staging`, `presigned`, `eager`, `stateless`, `транзиентной`, `задиспатчат`. Это нарушает `docs/rules.md` строка 10. Видно, например, в файлах `Media.php`, `MediaUrlService.php`, `MediaUploadPlanner.php`, `RemoveMediaOriginalHandler.php`, `app/src/Modules/Media/README.md` и тестах.

+ Добавить недостающий тест для пользовательского профиля: аватар должен подставлять значение по умолчанию, если медиа аватара уже в `readyOriginalRemoved`. Низкоуровневый тест `FindMediaOriginalUrlHandlerTest` есть, но на границе `UserPublicProfileAssembler` сейчас покрыты только «нет аватара», `ready` и «не готово».

+ Добавить недостающий тест на доменный метод `Media::markReadyOriginalRemoved()`: прямой вызов из неготового состояния должен падать, а повторный вызов на уже `ReadyOriginalRemoved` должен быть либо явным no-op, либо явно запрещён. Сейчас метод безусловно меняет статус и двигает `updatedAt`.

~ Переформулировать замечание 1: оно верное, но слишком узкое. Проблема не только в отсутствии guard из `Ready`, а в том, что сам доменный переход не защищает контракт удаления оригинала: источник состояния, повторный вызов и, возможно, наличие остающегося контента. Обработчик проверяет это снаружи, но сущность может быть вызвана напрямую.

~ Переформулировать замечание 2: факт про окно битой ссылки подтверждается. Но это не просто «на усмотрение»: порядок `deleteObject -> run()` конфликтует с правилом про надёжные шаги после сохранения бизнес-изменения через outbox. Лучше описать как архитектурный риск/нарушение надёжности: сначала фиксировать `readyOriginalRemoved` и событие удаления в одной транзакции, затем удалять объект отдельной идемпотентной задачей.

~ В замечании 2 убрать слабую формулировку «тестов не требуется, если оставить как есть». Если риск оставляют осознанно, нужен хотя бы тест/документированный сценарий на сбой `deleteObject` и на сбой `run()` после удаления, иначе это поведение не закреплено.

- Оба основных замечания ревью убирать не надо: факты подтверждаются кодом. `markReadyOriginalRemoved()` действительно без проверки, а `RemoveMediaOriginalHandler` действительно удаляет объект до сохранения статуса.

? План не проверял и не искал, потому что в ревью явно `plan: none`, а ты отдельно запретил угадывать файл плана. Тесты и PHPStan не запускал: это была проверка ревью по diff и коду.
