- [риск] `MediaConversionPlan` допускает пустые `video`/`audio` списки, но цель требует нормализованный mp4/m4a и волну аудио — явно решить: для video/audio требовать ровно один профиль или описать поведение без конверсий

- [риск] дубли профилей в одном списке приведут к падению по unique `(media_id, type)` уже после S3-записей — добавить проверку дублей в `CompleteMediaUploadHandler` и тесты на 422

- [риск] если video/audio-процессор загрузит файлы в S3, а потом `copyObject()` или `entityManager->run()` упадёт, `DeleteMediaHandler` их не удалит, потому что строк конверсий в БД ещё нет — добавить уборку по детерминированным путям или откат загруженных объектов при ошибке

- [риск] для временной ошибки ffmpeg план меняет только `$isTransient`, но `RetryException` всё равно получит текст `Ошибка хранилища при обработке медиа.` — сделать безопасный текст повтора по типу исключения

- [риск] `MediaUploaded{plan}` через Valinor реализуем, но только если у `MediaConversionPlan` будут точные PHPDoc-типы `list<...>` на всех трёх списках — явно добавить это в план и покрыть round-trip тестом

- [риск] `php-ffmpeg` умеет штатно делать PNG-волну, а плану нужен массив чисел 0..255 — описать конкретный способ получения PCM/амплитуд, возможно через отдельную обёртку над `ffmpeg`-процессом, и покрыть это тестом

- [риск] автоповорот видео и очистка rotation metadata не раскрыты достаточно конкретно — зафиксировать точные ffmpeg-аргументы или фильтры, иначе можно получить видео с неправильной ориентацией

- [архитектура] `Infrastructure`-процессор может зависеть от `MediaFileServiceContract`: это не нарушение текущей `docs/arch.md`, потому что `Infrastructure -> Contract своего модуля` разрешён — но в плане надо явно закрепить, что процессор не ходит в репозитории и не строит доменные сценарии

- [конкретика] `MediaWaveformTypecast` надо делать именно как `ColumnValueTypecast` со статическими `castDatabaseValue()` и `uncastValue()`, иначе общий `ValueObjectCast` его не вызовет — добавить это как критерий готовности, не только как пример

- [риск] `MediaPath::originalReady()` сейчас бросает для `Audio`; план это учитывает, но тест должен проверять не только `audios/source`, а ещё `assertValid` для `audios/...` и отказ для `Document`

- [риск] `FindMediaUrlHandler` остаётся только для оригинала, а именно его используют Posts/User для отображения медиа — решить, должны ли ленты получать нормализованные video/audio URL, и если да, расширить `FindMediaUrl` или слой сборки Posts

- [тесты] тесты плана покрывают `GetMediaUrl`, но не покрывают смежный пользовательский путь через `PostViewAssembler -> FindMediaUrl` — добавить тест или явно записать, что посты пока показывают оригинал

- [тесты] для 100% покрытия ffmpeg-процессоров одного интеграционного теста мало: нужны ветки успеха, ошибка скачивания, ошибка ffmpeg, ошибка загрузки, удаление временных файлов в `finally`, временная и постоянная классификация — перечислить их как обязательные

- [тесты] план говорит про маленькие mp4/audio фикстуры, но не говорит, где они лежат и как обновляются — добавить фиксированный источник: committed fixture или генерация в тесте через ffmpeg

- [правила] в плане и будущих логах нельзя писать «транзиентная», «ретрай», «payload», «seam» в человекочитаемом русском тексте — заменить на «временная», «повтор», «содержимое сообщения», «обёртка»

- [правила] `MediaBitrate` сейчас содержит `NAME = 'Bitrate видео'`, это уже нарушает правило «без англицизмов» и станет заметнее при аудио — заменить на «Битрейт» вместе с тестом сообщений

- [факт] у модуля Media действительно нет HTTP-контроллеров, поэтому роут-интеграционные тесты для новых `GetMediaUrl`/`GetAudioWaveform` не нужны — оставить только Application/Feature тесты

- [источник] Packagist подтверждает `php-ffmpeg/php-ffmpeg v1.4.0` с поддержкой PHP 8.5 и требованием бинарей `ffmpeg`/`ffprobe`: https://packagist.org/packages/php-ffmpeg/php-ffmpeg — зависимость выбрана реализуемо, но Docker-сборка обязана проверять оба бинаря
