# Кросс-CLI мета-ревью (codex) — 2026-06-19_20-30_posts-application-presentation

Запуск: `codex exec` в strict-режиме над `docs/reviews/2026-06-19_20-30_posts-application-presentation.md`.
Target: `git diff HEAD` (незакоммиченные изменения Posts/Tags/User/Media + тесты). План:
`docs/plans/2026-06-18_17-42_posts-application-presentation.md`.

## Ответ codex

+ Добавить: `CommentViewAssembler::fromComments()` имеет тот же риск, что пункт 3: авторы комментариев берутся из карты без проверки ключа. Это затрагивает списки комментариев и ответов, не только ленту записей.

+ Добавить: в ленте медиа не полностью «без N+1». `PostViewAssembler` пакетно берёт `post_media`, но потом для каждого вложения отдельно вызывает `FindMediaUrlHandler`, а тот отдельно читает `Media`. Тест `testFeedBatchesMediaAndTagsPerPost` проверяет только ответ, не число запросов/вызовов.

+ Добавить как риск User: `GetUserPublicProfile(s)` сейчас не скрывают `User::isDeleted()`. Поэтому «удалённый» пользователь при мягком удалении не пропадает, а продолжает попадать в авторов и получателей уведомлений. Если продуктово удалённые аккаунты должны исчезать из публичного профиля, это отдельная пропущенная проблема.

− Убрать из пункта 1 утверждение, что текущий сценарий «упомянутый удалён → публикация падает 422» прямо воспроизводится. Сейчас мягко удалённый пользователь всё равно вернётся профилем, а физическое удаление пользователя из `post_mentions` каскадно удалит строку упоминания. То есть 422 возможен только при будущем изменении контракта профилей или порче данных.

− Убрать из пункта 3 формулировку «если автор записи исчез» как текущий обычный поток: физическое удаление автора записи запрещено FK `posts.user_id RESTRICT`. Оставить можно только как латентную хрупкость при изменении политики профилей или неконсистентных данных.

~ Пункт 2 оставить, но переформулировать: это не просто «лишний батч», а дорогая проверка существования через полный публичный профиль и аватар. Полностью убрать нельзя, потому что валидация упоминаний при создании черновика нужна.

~ Пункт 4 оставить только если привязать к реальному риску из пунктов 1/2: нужен общий resolver с двумя режимами, строгим для входных упоминаний и мягким для рассылки по уже сохранённым. Как абстрактное «дублирование приватных методов» это слабое замечание для 4-го круга.

~ Пункт 5 оставить как очень низкий приоритет. Лучше уточнить: прямой вызов `FindMediaUrlHandler` не нарушает границу модулей, но обходит `QueryBus` и его обвязку; если это сделано ради `null`, это надо явно написать так же, как в `UserPublicProfileAssembler`.

~ Блок про фиксы 3-го круга можно оставить: `array_unique` для `mediaIds`, перенос `post_mention` на публикацию черновика, guard пустого `findByTexts`, `UserCollection::map()` действительно на месте. Новых регрессий по этим четырём пунктам не увидел.

? Тесты я не запускал, проверка по коду и тестам статически. Для N+1 по медиа нужен отдельный тест со счётчиком запросов или подменой `FindMediaUrlHandler`.

## Что применено основным редактором

- Схема FK подтверждена по `app/database/migrations/20260617.160942_0_create_posts_domain_tables.php`:
  `posts.user_id`/`comments.user_id` `delete: RESTRICT`; `post_mentions.user_id` `delete: CASCADE`;
  `UserRepository::findByIds` не фильтрует soft-delete. → п.1 и п.3 переквалифицированы в латентные.
- `CommentViewAssembler::fromComments:59` подтверждён — п.3 расширен.
- N+1 по URL медиа подтверждён (`PostViewAssembler::mediaItem`) — добавлен п.6.
- Спекулятивный пункт про скрытие `isDeleted()` отклонён как сквозной продуктовый вопрос вне диффа.
