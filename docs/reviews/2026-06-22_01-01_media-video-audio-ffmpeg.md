---
title: Media — реальная нарезка видео и аудио на ffmpeg
date: 2026-06-22 01:01
target: git diff HEAD + untracked (рабочее дерево)
plan: docs/plans/2026-06-20_21-48_media-video-audio-ffmpeg.md
mode: strict
score: 84
status: final
meta_reviewers: [plan-check (sonnet), architecture-check (sonnet), rules-check (sonnet), quality-check (sonnet), codex]
---

# Ревью: Media — реальная нарезка видео и аудио на ffmpeg

## Оценка

**84/100.** Реализация плотно следует плану: появился полный аудио-доменный стек, таблица
`media_audio_conversions`, реальные ffmpeg-процессоры видео и аудио, ветвление обработки по типу,
выдача URL для трёх типов и отдельная волна. Код аккуратный, в основном соответствует правилам (VO,
enum, исчерпывающие `match`, ранние возвраты, typecast-слой). Снижение оценки — за два реальных дефекта
(утечка временного файла при сбое скачивания; плоские нули в волне при `waveformPeaks > sampleCount`),
несколько нарушений правил (голый `\RuntimeException` в guard-clause процессоров, англицизмы в русских
докблоках/комментариях/README, `handle()` длиннее лимита), заметное отклонение от стратегии логирования
плана (`debug_precise` требовал DEBUG-логов в процессорах и Query-обработчиках, а логгер туда вообще не
инжектится) и расхождение CQRS-контракта Query Handler с конвенцией возврата. Все пункты — «на усмотрение
автора»; блокирующих «править обязательно» нет. После кросс-CLI из ревью убраны три пункта как
недоказанные/несуществующие (дубли типов, файл без расширения, недостижимая ветка Document, зависимость
от `instanceof Video`).

## Проблемы сверки с планом

### 1. Поведение «решателя» временной ошибки уже, чем в плане

План (фаза 3, раздел «Ошибки ffmpeg») перечисляет как временные (ретраябельные) ошибки не только
таймаут транскодирования, но и «нехватку ресурсов процесса». Это же повторено в докблоке нового
исключения. Фактически реализованный классификатор считает временной ошибкой исключительно таймаут —
любая нехватка ресурсов, нештатное завершение процесса по нехватке памяти и т.п. попадут в «постоянную»
ветку и уедут в `failed` без повтора.

Само по себе это может быть осознанным сужением (надёжно отличить «временную нехватку ресурсов» от
«битого входа» по исключению php-ffmpeg тяжело). Но тогда расхождение стоит зафиксировать: либо
поправить формулировку в докблоке `MediaProcessorFailedException`, чтобы она не обещала поведение,
которого нет, либо явно расширить классификатор. Сейчас докблок и код расходятся, и при эксплуатации
это введёт в заблуждение. (Уточнение мета-ревью: README такого обещания НЕ содержит — его текст
описывает только «таймаут транскодирования → временная», поэтому README в правке не нуждается; проблема
строго в докблоке `MediaProcessorFailedException.php:28`.)

Технические детали:

- **Статус:** отклонение
- **Где:** `app/src/Modules/Media/Infrastructure/FileService/AbstractFfmpegMediaProcessor.php:82` —
  `isTransientFailure()` ловит только `ProcessTimedOutException`; докблок
  `app/src/Modules/Media/Application/Exception/MediaProcessorFailedException.php:28` обещает
  «нехватку ресурсов процесса» как временную.
- **Что подтверждает проблему:** цикл по `getPrevious()` возвращает `true` только для
  `ProcessTimedOutException`; ветки для OOM/ресурсов нет.
- **Как исправить:** выбрать одно из двух — либо привести текст докблока к фактическому поведению
  («временная только при таймауте»), либо добавить распознавание сигнала нехватки ресурсов и юнит-тест
  на него. Минимально достаточно правки текста докблока.

## Замечания

### 1. Временный файл оригинала может остаться на диске при сбое скачивания

Когда видео/аудио процессор скачивает оригинал, частичный файл может остаться внутри самого
`downloadToFile` при сбое скачивания. Механика (уточнена мета-ревью): процессор вызывает
`downloadToFile` ПЕРВОЙ строкой, ещё до входа в свой `try`. Если скачивание упадёт (штатный сценарий —
объект конкурентно удалили или кратковременный сбой сети), S3-клиент уже мог записать частичный файл на
диск через `SaveAs`. `downloadToFile` бросает исключение, не удалив этот файл, и путь наружу не
возвращается. При этом переменная `$sourceFile` в процессоре не присваивается (оператор `=` не
завершился), поэтому `finally` процессора к ней обратиться не может в принципе — то есть очистить
частичный файл некому ни в процессоре, ни в `downloadToFile`. Файл останется в системном temp-каталоге
навсегда. Источник проблемы и место исправления — строго внутри `downloadToFile`.

На горячем пути обработки медиа это медленно, но верно засоряет диск worker-а временными файлами,
особенно при всплесках сетевых ошибок S3. Риск проявится не сразу, а как постепенное заполнение
`/tmp` под нагрузкой с ретраями.

Технические детали:

- **Тип:** `bug`
- **Рекомендация:** на усмотрение автора
- **Где:** `app/src/Modules/Media/Infrastructure/FileService/S3MediaFileService.php:216`
  (метод `downloadToFile`, тело `try/catch` ниже): создаёт `$localFile`, при `AwsException` бросает, не
  удаляя файл. Процессоры (`FfmpegMediaVideoProcessor.php:44`, `FfmpegMediaAudioProcessor.php:45`)
  вызывают `downloadToFile` до своего `try`, поэтому их `finally` тут не помогает.
- **Что подтверждает проблему:** `getObject(['SaveAs' => $localFile])` может создать частичный файл до
  ошибки; в `catch (AwsException)` нет очистки `$localFile`, а процессор получит исключение до того, как
  узнает путь.
- **Как исправить:** в `downloadToFile` обернуть скачивание в `try { ... } catch (AwsException) { удалить
  частичный файл, если создан; перебросить }` — это законная очистка ресурса на границе Infrastructure
  (rules.md разрешает try-catch в инфраструктуре, если он реально освобождает ресурс). Без выноса наружу
  нового типа исключения.
- **Тесты:** Feature/Infrastructure против MinIO. Важно (уточнение кросс-CLI): простой тест «скачивание
  несуществующего объекта не оставляет файл» может НЕ поймать баг — S3 часто падает ДО создания
  `SaveAs`-файла, и утечки не будет. Нужен сценарий, где частичный `$localFile` уже создан, а затем
  `getObject()` бросает `AwsException` (например, оборвать поток/подменить клиент так, чтобы файл
  появился, а вызов упал), и проверить отсутствие temp-файла после.

### 2. Прямые вызовы ffmpeg через Process не покрыты юнит-тестами и зависят от деталей библиотеки

Извлечение постера видео и волны аудио сделано прямыми вызовами бинаря ffmpeg через
`Symfony\Component\Process\Process` внутри `runEncoding()`. Юнит-тесты подменяют весь `runEncoding()`
целиком, поэтому эти куски проверяются только интеграционным тестом в Docker. Это допустимо по плану, но
стоит держать в голове: логика разбора PCM (перевод беззнаковых сэмплов в знаковые, нормализация в
0..255, деление на интервалы) — нетривиальная и сейчас не имеет отдельного быстрого теста; её корректность
держится на одном интеграционном прогоне с сигналом 440 Гц.

Отдельный граничный риск той же арифметики (отмечен мета-ревью): при `$sampleCount < $peaks` часть
интервалов получает `$start === $end`, внутренний цикл не выполняется ни разу, и `$maxAmplitude`
остаётся 0 — пустые интервалы молча дают амплитуду 0. На сигнале 440 Гц (`WAVEFORM_SAMPLE_RATE = 8000`,
64 пика) `sampleCount` всегда много больше `peaks`, поэтому проблема не проявляется; но `waveformPeaks`
валиден вплоть до 4096 (`MediaWaveformPeakCount` MAX), и короткое аудио этот случай реально допускает —
тогда результат будет тихо некорректным (плоские нули вместо реальных пиков). Кросс-CLI рекомендует
трактовать это не как тестовый пробел, а как реальный дефект результата при граничном входе: скрытое
предусловие `$sampleCount >= $peaks` без guard и без комментария.

Технические детали:

- **Тип:** `bug` (граничный результат) + `tests` (пробел покрытия)
- **Рекомендация:** на усмотрение автора
- **Где:** `app/src/Modules/Media/Infrastructure/FileService/FfmpegMediaAudioProcessor.php:137`
  (`buildWaveform` — разбор PCM); `:152–167` (деление на интервалы, незащищённое от `$peaks > $sampleCount`);
  `FfmpegMediaVideoProcessor.php:144` (`extractPoster` прямым Process).
- **Что подтверждает проблему:** юнит-тест `FfmpegMediaProcessorTest` подменяет `runEncoding()`, значит
  `buildWaveform`/`extractWaveform`/`extractPoster` в нём не исполняются; покрытие даёт только
  `FfmpegMediaProcessorIntegrationTest`, проверяющий лишь длину волны и диапазон, но не сами амплитуды и
  не граничные случаи.
- **Как исправить:** опционально вынести чистый `buildWaveform(string $pcm, int $peaks)` под отдельный
  быстрый юнит-тест с заранее известным PCM-буфером (граничные сэмплы −32768/32767, пустой буфер,
  `$peaks > $sampleCount`, неравномерное деление) — это покроет арифметику без бинаря и зафиксирует
  контракт нормализации. При желании — комментарий или guard о предусловии `$sampleCount >= $peaks`.
- **Тесты:** добавить unit на `buildWaveform` (если метод сделать тестируемым) с детерминированными
  входами, включая короткий буфер.

### 3. Типизированное исключение в typecast волны — `\InvalidArgumentException`, а не доменное

Новый typecast волны при встрече битого JSON или нецелого значения бросает
`\InvalidArgumentException`. Правила проекта требуют, чтобы guard-clause бросал типизированное доменное
исключение, а не generic-заглушку. (Уточнение мета-ревью по источнику правила: специфическое правило
«VO бросают `InvalidDomainValueException`» относится к самим VO и доменным коллекциям, а не к
инфраструктурному typecast-слою; применимое здесь правило — общее «если ошибку код обнаруживает сам
(guard-clause, проверка предусловия), он сразу бросает конкретное типизированное доменное исключение, а
не generic-заглушку вроде `\UnexpectedValueException`/`\RuntimeException`».) Для typecast подходящий
выбор — `InvalidDomainValueException` (данные в БД несовместимы с доменной моделью → 500). Итоговый
вывод не меняется: голый SPL-тип здесь нежелателен.

Это сознательно скопировано из соседнего `MediaMultipartPartCollectionTypecast` и согласуется с общим
`ValueObjectCast`, который тоже бросает `\InvalidArgumentException` в typecast-слое, — то есть это уже
сложившийся в проекте паттерн инфраструктурного typecast, а не самовольное новшество. Поэтому это не
однозначное нарушение для одного нового класса и не регресс, а вопрос консистентности всего слоя: если
команда считает существующий паттерн допустимым отклонением для typecast (ветка «битые данные в БД», 500
в любом случае), стоит зафиксировать это решение явно, а не молча тиражировать. Замечание мягкое.

Технические детали:

- **Тип:** `rules`
- **Рекомендация:** на усмотрение автора
- **Где:** `app/src/Modules/Media/Infrastructure/Cycle/MediaWaveformTypecast.php:21,27`; образец —
  `MediaMultipartPartCollectionTypecast.php:23,29,36`.
- **Что подтверждает проблему:** rules.md «если ошибку код обнаруживает сам (guard-clause)… бросает
  конкретное типизированное доменное исключение, а не generic-заглушку» и «Типизированные доменные
  исключения вместо `RuntimeException`» ориентируют на `InvalidDomainValueException`; здесь и в образце —
  голый SPL-тип.
- **Как исправить:** либо заменить на `InvalidDomainValueException` (тогда заодно стоит выровнять и
  существующий `MediaMultipartPartCollectionTypecast`, чтобы модуль был единообразен), либо оставить как
  есть, но осознанно — это решение для всего typecast-слоя, а не для одного класса.

### 4. Голый `\RuntimeException` в guard-clause процессоров вместо типизированного исключения

Процессоры внутри `runEncoding()` отбрасывают вход неверного типа голым `\RuntimeException`:
аудио-процессор — для файла с видеодорожкой, видео-процессор — для файла без видеодорожки. Это
достижимые guard-clause (проверка предусловия, которую код обнаруживает сам), а rules.md прямо
запрещает для них generic-заглушки: «если ошибку код обнаруживает сам (guard-clause, проверка
предусловия), он сразу бросает конкретное типизированное доменное исключение в источнике, а не
generic-заглушку вроде `\UnexpectedValueException`/`\RuntimeException`».

Смягчающий фактор: оба `\RuntimeException` ловятся в `encode()` через `catch (\Throwable)` и
переупаковываются в `MediaProcessorFailedException::permanent()`, поэтому наружу голый тип не уходит и
поток корректен. Но правило относится к источнику выброса, а не к итоговому типу, — значит нарушение
формально остаётся. Те же недостижимые ветки (нет потока/нет PCM) тоже на голом `\RuntimeException`, но
они менее критичны (помечены `@codeCoverageIgnore`).

Технические детали:

- **Тип:** `rules`
- **Рекомендация:** на усмотрение автора
- **Где:** достижимые — `FfmpegMediaAudioProcessor.php:87` («Загруженный файл не является аудио.»),
  `FfmpegMediaVideoProcessor.php:104` («Загруженный файл не является видео.»); недостижимые —
  `FfmpegMediaAudioProcessor.php:99,144`, `FfmpegMediaVideoProcessor.php:123`.
- **Что подтверждает проблему:** строки 87/104 исполняются в интеграционных тестах
  (`testAudioProcessorRejectsVideoSource`/симметричный для видео) — это реальный, не теоретический путь.
- **Как исправить:** бросать в этих guard сразу `MediaProcessorFailedException::permanent(...)` (или
  `InvalidDomainValueException`), а не голый `\RuntimeException` с последующей переупаковкой.

### 5. Англицизмы-кальки в русских докблоках, комментариях и README

rules.md явно запрещает кальки с английского в человекочитаемом русском тексте и приводит образец
«транзиентный» → «временный». В коде модуля рассыпаны «транзиентный/транзиентности/ретраябельный»,
«throttling», «scope», «trade-off», «retry/terminal», «payload» в прозе — это те же кальки, которые
правило называет запрещёнными. Технические идентификаторы (имя поля `payload`, AWS-коды
`Throttling`/`ThrottlingException`/`RequestThrottled` как строковые литералы) под правило НЕ попадают и
трогать их не нужно — речь только о русском прозаическом тексте.

Технические детали:

- **Тип:** `rules`
- **Рекомендация:** на усмотрение автора
- **Где:** `MediaFileServiceFailedException.php:9` («признак транзиентности»), `:27` («Транзиентный
  (ретраябельный) сбой… throttling»); `MediaProcessorFailedException.php:28` («(ретраябельный)»);
  `AbstractFfmpegMediaProcessor.php:84` («(ретраябельный)»);
  `RecordMediaProcessingFailureHandler.php:15` («транзиентной»); `Media.php:219` («после транзиентной
  ошибки»); `ProcessMediaJob.php:24` («транзиентную»), `:65` («retry/terminal»), `:95`
  («ретраябельный»); `MediaTypeResolver.php:13` («вне scope»); `README.md:43` («outbox-payload»),
  `:80,81,94` («транзиентности», «транзиентная», «throttling»), `:99` («Trade-off»). Те же кальки есть в
  тестовых комментариях `ProcessMediaJobTest.php:94`, `MediaProcessingFlowTest.php:96` (дополнено
  кросс-CLI).
- **Что подтверждает проблему:** rules.md (раздел «Язык», «Без англицизмов») приводит ровно пример
  «транзиентный» → «временный».
- **Как исправить:** заменить на «временный»/«повторяемый»; «throttling» в русском докблоке — на
  «ограничение скорости», «scope» — «область»/«рамки», «trade-off» — «компромисс», «retry/terminal» —
  «повтор/терминальный исход» (английское слово оставить, только если это строковый литерал AWS-кода).

### 6. `ProcessMediaHandler::handle()` длиннее лимита коротких методов

rules.md: «`handle()` в Handler — не более ~40 строк». Метод `handle()` в `ProcessMediaHandler`
(строки 51–112) — около 62 строк. Декомпозиция частично есть (`buildImageConversions`/
`buildVideoConversions`/`buildAudioConversions`), но сам `handle()` всё равно за пределом: помимо
`match` он несёт логирование, перекладку оригинала, `markReadyMovedTo`, цикл `persist` и финальный
лог. Это менее строгое замечание (метод читаемый), но формально лимит превышен.

Технические детали:

- **Тип:** `rules`
- **Рекомендация:** на усмотрение автора
- **Где:** `app/src/Modules/Media/Application/Command/ProcessMedia/ProcessMediaHandler.php:51-112`.
- **Что подтверждает проблему:** ~62 строки тела против лимита ~40.
- **Как исправить:** при желании вынести финальную часть (перекладка оригинала + `markReadyMovedTo` +
  flush + итоговый лог) в приватный метод `persistReady(...)`, оставив `handle()` тонким.

### 7. `GetAudioWaveformHandler` возвращает доменный VO напрямую, минуя конвенцию возврата Query

arch.md (раздел «Правила CQRS»): «Query Handler возвращает Entity, типизированную коллекцию, Result
DTO или `PaginatedResult<T>`». `GetAudioWaveformHandler::handle()` возвращает `MediaWaveform` —
доменный VO, который в этот список не входит. План сам предписал такой возврат (разделы «Целевой
алгоритм»/«Контракты реализации»: `GetAudioWaveformHandler → MediaWaveform`), поэтому это не
самовольное отклонение. HTTP-входа у Media сейчас нет (уточнение кросс-CLI), так что речь не о том, что
наружу уже течёт сырой VO; фактическое отклонение — только в том, что Query Handler возвращает Domain-VO
вместо Result DTO/Entity, без явного обоснования исключения.

Технические детали:

- **Тип:** `arch`
- **Рекомендация:** на усмотрение автора
- **Где:** `app/src/Modules/Media/Application/Query/GetAudioWaveform/GetAudioWaveformHandler.php:25,43`
  (`: MediaWaveform` и `return $conversion->waveform`).
- **Что подтверждает проблему:** возвращаемый тип — `App\Modules\Media\Domain\ValueObject\MediaWaveform`,
  это не Entity/коллекция/Result DTO/`PaginatedResult`.
- **Как исправить:** либо завернуть в `GetAudioWaveformResult` (Result DTO), либо зафиксировать в плане/
  доке осознанное исключение из конвенции для этого Query.

### 8. Дублирование шаблона и необъяснённые «магические» числа в ffmpeg-процессорах

Видео- и аудио-процессоры повторяют одинаковую обёртку `encode()` (`try { runEncoding } catch
(\Throwable) { classifyFailure }`) и одинаковую конвертацию `(int) \round(probeFloat(..., 'duration') *
1000)` (секунды → миллисекунды) без поясняющего комментария об единицах. В аудио-процессоре PCM-константы
(`PCM_SIGNED_OFFSET = 65536`, `PCM_FULL_SCALE = 32767`, `PCM_SIGNED_THRESHOLD = 32768`) даны без
комментария об их происхождении (ширина диапазона u16 / максимум s16). При добавлении третьего процессора
обёртка и конвертация скопируются ещё раз.

Технические детали:

- **Тип:** `quality`
- **Рекомендация:** на усмотрение автора
- **Где:** обёртка `encode()` — `FfmpegMediaAudioProcessor.php:64-74`, `FfmpegMediaVideoProcessor.php:76-92`;
  конвертация длительности — `FfmpegMediaAudioProcessor.php:107`, `FfmpegMediaVideoProcessor.php:133`;
  PCM-константы — `FfmpegMediaAudioProcessor.php:32-34`.
- **Что подтверждает проблему:** идентичная структура обёртки и одинаковый множитель `* 1000` в двух
  классах; PCM-константы без комментария о смысле значений.
- **Как исправить:** поднять шаблонный `encode()` и хелпер `probeDurationMs()` в
  `AbstractFfmpegMediaProcessor`; добавить однострочные комментарии к PCM-константам.

### 9. Логирование расходится с планом `debug_precise`: нет DEBUG в процессорах и Query-обработчиках, итоговый счётчик не по типам

План (раздел «Логирование», стратегия `debug_precise`) детально расписывает DEBUG-логи: в ffmpeg-
процессорах («оригинал скачан», результат probe, «транскод готов», «постер/волна извлечены»), в
`GetMediaUrlHandler`/`GetAudioWaveformHandler` («DEBUG на резолв»), а итоговый DEBUG «медиа готово» —
«со счётчиками конверсий по типам». Фактически (расширено кросс-CLI): процессоры и оба Query-обработчика
вообще не получают `LoggerInterface` в конструкторе, то есть ни одного из заявленных DEBUG-логов там
нет; а итоговый лог `ProcessMediaHandler` содержит одно поле `'conversions' => \count($conversions)` без
разбивки по image/video/audio. Образец правильной разбивки по типам — рядом в
`CompleteMediaUploadHandler` (`imageConversions`/`videoConversions`/`audioConversions`).

Технические детали:

- **Тип:** `rules`
- **Рекомендация:** на усмотрение автора
- **Где:** нет `LoggerInterface` — `AbstractFfmpegMediaProcessor.php:24-27` (конструктор),
  `FfmpegMediaAudioProcessor.php` / `FfmpegMediaVideoProcessor.php` (наследуют конструктор),
  `GetMediaUrlHandler.php` и `GetAudioWaveformHandler.php:20-23` (конструкторы без логгера); итоговый
  счётчик одним числом — `ProcessMediaHandler.php:107-111`.
- **Что подтверждает проблему:** план `debug_precise` перечисляет конкретные DEBUG-логи в этих местах,
  но в коде логгер туда не инжектится; контекст финального лога — единое число `\count($conversions)`,
  тогда как план требует «счётчики по типам». Образец правильной формы рядом в
  `CompleteMediaUploadHandler.php:79-81`.
- **Как исправить:** либо добавить заявленные планом DEBUG-логи (инжектить логгер в процессоры/Query и
  разбить итоговый счётчик по типам), либо явно скорректировать стратегию логирования в плане до
  фактической, если детальные DEBUG там сочли избыточными.

## Рекомендации

- **Править обязательно:** —
- **На усмотрение автора:** план 1, замечания 1, 2, 3, 4, 5, 6, 7, 8, 9

## Изменения после мета-ревью

Режим: strict. Запущены subагенты plan-check, architecture-check, rules-check, quality-check (sonnet).

### После plan-check / architecture-check / rules-check / quality-check

_(Нумерация ниже — на момент этого этапа; после кросс-CLI часть пунктов удалена и список
перенумерован — см. следующий блок и финальную нумерацию в теле ревью.)_

- **+ Добавлено:**
  - Голый `\RuntimeException` в достижимых guard-clause процессоров
    (`FfmpegMediaAudioProcessor.php:87`, `FfmpegMediaVideoProcessor.php:104`) вместо типизированного
    исключения — нарушение rules.md про guard-clause (подтверждено rules-check и quality-check).
  - Англицизмы-кальки «транзиентный/ретраябельный/throttling» в русских докблоках/комментариях/README —
    rules.md прямо запрещает (подтверждено grep).
  - `ProcessMediaHandler::handle()` ~62 строки > лимита ~40 (rules-check).
  - `GetAudioWaveformHandler` возвращает доменный VO напрямую, минуя CQRS-конвенцию возврата Query
    (architecture-check; план это санкционировал, но обоснования в коде нет).
  - Дублирование обёртки `encode()` и конвертации длительности между процессорами + PCM-константы без
    комментария о происхождении (quality-check).
  - Итоговый DEBUG в `ProcessMediaHandler` не разбивает конверсии по типам — отклонение от плана
    `debug_precise` (plan-check).
  - Зависимость от `instanceof Video` вынесена в отдельный пункт (позже удалена кросс-CLI).
- **~ Изменено:**
  - План 1: убрано ложное обвинение README — README обещания «нехватка ресурсов процесса» НЕ содержит,
    проблема строго в докблоке `MediaProcessorFailedException.php:28` (plan-check).
  - План 2: исправлен номер строки `CompleteMediaUploadHandler` 184 → 139 (`assertImagePlan`).
  - Замечание 1: уточнена механика утечки (при сбое `downloadToFile` `$sourceFile` в процессоре не
    присваивается, `finally` процессора неприменим, очистка возможна только внутри `downloadToFile`);
    исправлен номер строки 948 → 216/220 (plan-check, rules-check, architecture-check).
  - Замечание 2: добавлен граничный риск арифметики волны (`$peaks > $sampleCount` → пустые интервалы
    молча дают 0), уточнены тест-кейсы (quality-check).
  - Замечание 3: уточнён источник правила — применимо общее правило про guard-clause, а не VO-специфичное;
    вывод не изменился (rules-check, architecture-check).
  - Замечание 4: понижена значимость — риск умозрительный, входные форматы ограничены; исправлен номер
    строки 952 → 220 (quality-check).
  - Замечание 5: исправлен номер строки 177 → 123 и устранена фактическая ошибка — комментарий-маркер
    «защита инварианта» отсутствует в ОБОИХ местах (в `ProcessMediaHandler` его тоже нет) (quality-check).
- **− Убрано:** —
- **Отклонено:**
  - Снять замечание 4 полностью (quality-check предлагал): оставлено как низкоприоритетная памятка —
    это честная «точка для ручной перепроверки», а не ложное замечание.
  - `MediaConfig` инжектится в `AbstractFfmpegMediaProcessor` (architecture-check `?`): не нарушение —
    Infrastructure свободно использует config/runtime-библиотеки; ограничение arch.md касается инъекции
    `TypedConfig` в Application, а не в Infrastructure.
  - `array`-свойства в `MediaConversionPlan` как нарушение (architecture-check сам снял): это допустимый
    `list<...Spec>` по rules.md, не нарушение.
  - `Collection::make($plan->image)` как untyped в `CompleteMediaUploadHandler:136` (quality-check):
    маргинально, PHPStan выводит тип из типизированного `list`; в ревью не вносим.

### После соседнего CLI (codex)

Кросс-CLI: Codex CLI 0.141.0 (`docs/reviews/2026-06-22_01-01_media-video-audio-ffmpeg_cli_meta.md`).

- **+ Добавлено:**
  - Расширено замечание о логировании: процессоры и оба Query-обработчика вообще не получают
    `LoggerInterface` (план `debug_precise` требовал DEBUG-логов в них) — не только итоговый счётчик не
    по типам. Слито с пунктом про логирование (теперь замечание 9).
- **~ Изменено:**
  - Замечание 1: уточнён тест — простой «скачать несуществующий объект» может не поймать баг (S3 падает до
    создания файла); нужен сценарий с уже созданным частичным `$localFile` и затем `AwsException`.
  - Замечание 2: повышено с «тестового пробела» до реального граничного бага результата
    (`$peaks > $sampleCount` → плоские нули); `waveformPeaks` валиден до 4096, короткое аудио допускает.
  - Замечание 3: смягчено — `\InvalidArgumentException` в typecast это уже паттерн проекта
    (`MediaMultipartPartCollectionTypecast`, общий `ValueObjectCast`), не однозначное нарушение для одного
    класса.
  - Замечание 5 (англицизмы): дополнен список мест — `MediaTypeResolver.php:13` («scope»),
    `ProcessMediaJob.php:65` («retry/terminal»), `:95`, `README.md:43` («outbox-payload»), `:99`
    («Trade-off»), тестовые комментарии `ProcessMediaJobTest.php:94`, `MediaProcessingFlowTest.php:96`.
  - Замечание 7 (возврат VO из Query): убрана неточность «Presentation получит сырой VO» — HTTP-входа у
    Media нет; отклонение только в типе возврата Query Handler.
- **− Убрано:**
  - План 2 (дубли типов): не проблема — для video/audio требование «дубли → 422» фактически выполняется
    через «ровно один профиль» (`count() !== 1`), отдельная проверка `unique` не нужна. Подтверждено
    Codex и существом.
  - Замечание про `instanceof Video` (бывш. 2a): неверно — `FFMpeg::open()` имеет документированный
    `@return Audio|Video` (vendor `FFMpeg.php:87`) и возвращает `Video` при наличии видеопотока, то есть
    это контракт библиотеки, а не недокументированная деталь.
  - Замечание про файл без расширения (бывш. 4): недоказано — интеграционный тест гоняет mp4/wav через
    `downloadToFile` без расширения, для целевых форматов проблемы нет; ревью — только по проблемам.
  - Замечание про недостижимую ветку Document (бывш. 5): нормальный защитный `match` по enum,
    соответствует плану; отсутствие комментария — не реальная проблема.
- **Отклонено:**
  - Полностью отклонять «реальные баги make test/phpstan» Codex не предлагал; помечен только `?` —
    фактический прогон `make test`/`make phpstan` он не делал (как и положено мета-ревью, проверки кода
    не запускаем).

## Применённые фиксы
Отчёт: `docs/review-fixes/2026-06-22_01-40_media-video-audio-ffmpeg.md`

Применены (режим apply-optional): баг 1, баг 2, план 1, замечания 4, 5, 6, частично 8 (комментарии к
PCM-константам), частично 9 (разбивка итогового DEBUG-счётчика по типам). Гейт `make qa` зелёный
(cs + phpstan + покрытие 100%).

Отклонённые optional-решения (не открывать повторно без новых аргументов):
- Замечание 3 (`\InvalidArgumentException` в `MediaWaveformTypecast`): это сложившийся паттерн всего
  typecast-слоя (`MediaMultipartPartCollectionTypecast`, общий `ValueObjectCast`); правка одного класса —
  рассогласование, правка всего слоя — расширение scope вне ревью. Решение об консистентности слоя.
- Замечание 7 (`GetAudioWaveformHandler` возвращает `MediaWaveform`): возврат прямо санкционирован
  планом; HTTP-входа у Media нет, сырой VO наружу не течёт. Обёртка в Result DTO противоречила бы плану.
- Замечание 8, часть «дедупликация `encode()`/`probeDurationMs()` в Abstract»: расширение scope и риск
  регрессии на горячем пути ради маргинальной выгоды; применена только дешёвая часть (комментарии).
- Замечание 9, часть «инъекция `LoggerInterface` в процессоры и оба Query-обработчика + DEBUG-логи плана»:
  большой объём изменений и отклонение от фактической реализации плана ради DEBUG; применена только
  дешёвая часть (счётчик по типам).
