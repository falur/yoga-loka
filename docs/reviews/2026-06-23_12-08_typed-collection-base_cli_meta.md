# Кросс-CLI мета-ревью (codex) — сырой ответ

Запущено на этапе strict для `docs/reviews/2026-06-23_12-08_typed-collection-base.md`.
`codex exec`, read-only по `git diff HEAD` + untracked + коду. `make test`/`make phpstan` не запускались.

---

+ Добавить: фаза со скиллом не подтверждается target-ом. План требует обновить `.claude/skills/laravel-collections/SKILL.md` (план:241), журнал и ревью пишут, что это сделано, но `.claude/` игнорируется через `.gitignore`, не попадает ни в `git diff HEAD`, ни в untracked-файлы. В коммит уйдёт только `docs/rules.md`, где уже есть ссылка на этот скилл (docs/rules.md:21). Риск сломанной документационной ссылки и незакрытого критерия плана.

+ Добавить: ревью пропустило ещё нарушения «Без англицизмов» в новом тексте `docs/rules.md`: «дженерик» (docs/rules.md:22) и «копипастом» (docs/rules.md:24). Замечание про `overfetch` в `CursorSlice` верное, но неполное.

+ Добавить: тесты `CursorSlice` не проверяют главный контракт про сохранение конкретного типа коллекции. В докстринге обещано, что `$items` будет той же типизированной коллекцией (CursorSlice.php:16), но тесты используют только голый `Illuminate\Support\Collection` (CursorSliceTest.php:16). Нужен тест с конкретным наследником `TypedCollection`.

− Убрать замечание 8 про `PostViewCollection`/`CommentViewCollection` как «не относящиеся к плану». Неверно: план перечисляет их в охвате (план:61).

− Убрать или слить с первым пунктом отдельное замечание про `MediaMimeType::equals()`. Журнал фиксирует, что тест добавлен по решению пользователя (journal:24). Как отдельная проблема слабое.

− Убрать замечание 3 про feature-тесты API как проблему. План разрешал альтернативу (план:224), журнал её выполнил (journal:23).

− Убрать замечание 7 про отсутствие комментария у `array_values(...->unique()->all())`. План явно разрешил это место (план:62); обязательного правила «комментируй исключение» нет.

~ Переформулировать замечание 2 про `CursorSlice::$items`: звучит как риск из-за PHPStan, но это нормальный способ выразить обобщённый тип в PHP. Лучше — тестовый пробел: нет теста, что `fromOverfetched()` возвращает конкретную типизированную коллекцию после `take()`.

~ Переформулировать пункт про смешанные изменения: риск верный, но замечание про отсутствие `Shared/Domain/Pagination` в `arch.md` слабое (arch.md:116 — общий доменный код живёт в `Shared/Domain`).

~ Оставить замечание про `GetTagsHandlerTest` (план:225) — верное.
~ Оставить замечание про `SendPushNotificationHandlerTest` (план:227) — верное.

? Не проверял выполнением `make test`/`make phpstan` — read-only мета-ревью.

---

## Решения главного редактора (применены в файле ревью)

- Принято: новое замечание 8 (`.claude/SKILL.md` под `.gitignore`), новое замечание 7 («копипастом»), переориентация замечания 2 на тестовый пробел `CursorSliceTest`.
- Принято удаление: прежнего замечания 8 (view-коллекции — план их перечисляет, строки 61/155–156), прежнего замечания 7 (комментарий к `unique()` — разрешено планом).
- Отклонено: «дженерик» как новый англицизм (устоявшийся термин, был до diff); полное удаление замечаний 1 и 3 (codex сам признаёт их частью проблемы смешанных изменений — оставлены как мягкие «на усмотрение»); усиление пункта arch.md (подано как осознанное расхождение).
