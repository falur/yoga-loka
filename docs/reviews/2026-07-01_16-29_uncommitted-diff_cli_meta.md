+ `tests/Unit/Modules/Media/Domain/Enum/MediaEnumTest.php:17-75` — новый enum `MediaConversionKind` не покрыт тестом значений, хотя остальные enum Media закреплены. При правиле 100% покрытия это пропущенный тест.

+ `app/src/Modules/Posts/Repository/PostMediaRepository.php:25-60` — ревью не фиксирует тестовый пробел: архитектура теперь требует eager-load `media.*Conversions`, но тесты проверяют в основном результат HTTP, а не сам контракт репозитория. Удаление `->load('media.imageConversions')`/`videoConversions`/`audioConversions` может пройти незаметно до появления превью.

− Замечание 1 лучше убрать как проблему в текущем коде: orphan-состояние `post_media.media_id` без строки `media` сейчас запрещено FK `RESTRICT` и уже покрыто `testCannotDeleteMediaReferencedByPostMedia`. Риск основан на будущей поломке схемы или ручной чистке, а не на достижимом поведении текущего дифа.

~ Замечание 3 сформулировано верно, но тяжесть занижена: `MediaUrlService::resolverFor()` нарушает прямое правило `rules.md` про исчерпывающий `match` по enum. Это не просто «на усмотрение», а обязательная правка, даже если паттерн унаследован из старого кода.

~ Замечание 4 смешивает две вещи. «Команда нигде не вызывается» само по себе не проблема. Проблема — если `RemoveMediaOriginal` уже считается публичным Application-сценарием, то порядок `deleteObject()` до фиксации статуса оставляет окно битой ссылки без надёжного повтора; это надо формулировать как архитектурный риск сценария, а не как риск отсутствующего входа.

~ Замечание 5 стоит уточнить: это не N+1 и не отдельная подпись ссылок на конверсии в ленте, а дополнительные eager-load выборки строк конверсий. Так как `docs/arch.md` прямо закрепляет такую загрузку под будущие превью, пункт должен звучать как осознанная стоимость архитектурного решения, а не как обычная оптимизация.

? Точное поведение Cycle при отсутствующей строке `media` за `PostMedia::$media` по коду доказать нельзя: нужен искусственный orphan в БД с отключённым или обойдённым FK. Поэтому утверждение из замечания 1 про гарантированный `Error` без такого воспроизведения остаётся недоказанным.
